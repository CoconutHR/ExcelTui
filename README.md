# Excel Tui

Excel通用数据提取工具,支持JSON、键值对、纯文本格式的数据提取和处理。

## 安装
```bash
pip install -e .
```

## 使用

在任意目录下运行:
```bash
exceltui
```

工具会自动扫描当前目录下的Excel文件。

## 功能特性

- 支持JSON格式数据提取
- 支持键值对格式 {key:value}
- 支持纯文本处理
- 多列交互式处理
- 转义字符处理
- 自定义输出格式