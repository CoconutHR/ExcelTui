# ExcelTui v5.0.0 — 功能介绍与使用说明

ExcelTui 是一款基于终端的 Excel 通用数据处理工具，提供丰富的 TUI 交互界面。所有 Excel 读写均使用纯文本格式，从源头杜绝长数字被 Excel 转为科学计数法的问题。v5.0 起完全移除 pandas 依赖，仅依赖 openpyxl + rich，安装体积更小、启动更快。

---

## 安装与启动

**安装依赖**（Python >= 3.7）：

```bash
pip install -e .
```

项目仅依赖 `openpyxl` 和 `rich` 两个第三方库，无需安装 pandas。

**启动工具**：

```bash
exceltui
```

或者在项目目录下运行：

```bash
python -m exceltui
```

启动后工具会自动扫描当前工作目录下的 Excel / JSONL 文件，通过交互式菜单引导操作。所有功能执行完毕后自动回到主菜单，随时可以继续处理下一项任务。

---

## 主菜单一览

```
┌─────────────────────────────────────────────────────┐
│  1. Excel 通用数据提取（JSON/键值对/纯文本）         │
│  2. 去除指定列的重复项                               │
│  3. 筛选交集（按另一文件顺序筛选）                   │
│  4. 合并当前文件夹下所有 Excel 文件                  │
│  5. 按指定行数分割 Excel 文件                        │
│  6. 按主键横向合并两个 Excel 文件                    │
│  7. 分组排序并标红最新时间行                          │
│  8. JSONL 解析为 Excel 文件                          │
│  9. 退出程序                                         │
└─────────────────────────────────────────────────────┘
```

---

## 功能详细说明

### 功能 1 · Excel 通用数据提取

适用于列中包含 JSON、`{key:value}` 键值对或纯文本的场景，可以将嵌套数据提取为独立的列。

**使用流程**：启动后工具列出当前目录的 xlsx 文件，选择目标文件 → 显示所有列，选择要处理的列 → 工具自动扫描该列的格式分布（JSON / 键值对 / 纯文本 / 空值比例），然后进入操作菜单。

**操作菜单**包含以下选项：

- **提取键值对到新列**：展示该列检测到的所有键名（支持分页，每页20个），用户选择要提取的键（可输入序号、键名或 `all` 全选），可自定义输出列名。支持处理转义字符（`\n`、`\t` 等）、去除首尾空白，以及选择"只写值"或"写入完整键值对"两种输出模式。
- **删除指定键值对**：从源列中移除选定的键值对。
- **处理纯文本列**：对纯文本内容进行转义字符处理和空白裁剪，生成新列。
- **切换到其他列**：在同一文件内切换处理不同的列。
- **保存并退出**：将所有修改写入新文件。
- **放弃修改并退出**：不保存任何更改。

**输出文件**：`{原文件名}_处理后.xlsx`。如果文件已存在会提示是否覆盖或自定义文件名。

---

### 功能 2 · 去除指定列的重复项

根据用户指定的列进行去重，保留每个值首次出现的行。

**使用流程**：选择文件 → 输入去重列的列字母（如 A、B、AA，支持小写自动转大写） → 确认操作 → 执行去重。完成后显示原始行数、去重后行数、删除的重复行数。

**输出文件**：`{原文件名}_去重_{列字母}列.xlsx`，例如 `data_去重_A列.xlsx`。

---

### 功能 3 · 筛选交集（按另一文件顺序筛选）

从数据文件中筛选出匹配筛选文件中值的行，并且输出顺序与筛选文件保持一致。

**使用流程**：

1. 选择数据文件（A 文件，被筛选的文件）
2. 选择筛选文件（B 文件，提供匹配值的文件）
3. 选择 A 文件中用于匹配的列
4. 选择 B 文件中提供匹配值的列

确认后执行筛选。结果按 B 文件值的出现顺序排列，同一匹配值对应多行时保持 A 文件中的原始顺序。完成后显示数据文件总行数、唯一匹配值数量、命中行数和未命中行数。

**输出文件**：`{A文件名}_筛选结果.xlsx`。

---

### 功能 4 · 合并当前文件夹下所有 Excel 文件

一键将当前目录下的全部 xlsx 文件纵向合并为一个文件。

**使用流程**：工具自动列出目录下所有 xlsx 文件（至少需要 2 个） → 展示将要合并的文件清单 → 输入输出文件名（默认 `合并结果.xlsx`） → 确认后执行。合并逻辑以第一个文件的表头为准，后续文件自动对齐列（多退少补）。如果输出文件本身也在目录中，会自动跳过。

**输出文件**：用户自定义，默认 `合并结果.xlsx`。

---

### 功能 5 · 按指定行数分割 Excel 文件

将一个大文件按指定行数拆分成多个小文件，每个子文件保留原始表头。

**使用流程**：选择文件 → 显示文件总行数 → 输入每份的行数（默认 5000） → 确认分割份数 → 执行分割。完成后展示每个子文件的文件名和行数汇总。

**输出文件**：`{原文件名}_第1份.xlsx`、`{原文件名}_第2份.xlsx`……以此类推。

---

### 功能 6 · 按主键横向合并两个 Excel 文件

根据主键列将两个文件横向拼接，类似数据库的 JOIN 操作。

**使用流程**：

1. 选择主文件（A 文件）
2. 选择追加文件（B 文件）
3. 选择 A 文件的主键列
4. 选择 B 文件的主键列

如果两个文件的主键存在不完全匹配的情况，工具会提示选择合并方式：**仅保留 A 文件的行**（left，默认）、**保留所有行**（outer）、**仅保留交集**（inner）。B 文件中主键列以外的列将追加到 A 文件右侧，列名重复时自动加 `_B` 后缀。同一主键在 B 文件中有多行时取第一行。

**输出文件**：`{A文件名}_横向合并.xlsx`。

---

### 功能 7 · 分组排序并标红最新时间行

按分组列对数据分组，在每组中找出时间最新的行，整体排序后将最新行标红高亮。

**使用流程**：选择文件 → 选择分组 ID 列（如 sessionId） → 选择时间列 → 工具自动检测时间格式（采样前 100 行，支持 9 种常见格式，至少 60% 匹配才算成功） → 显示分组数量并确认 → 执行处理。

**支持的时间格式**：`2025-04-24 14:30:00`、`2025/04/24 14:30:00`、`2025-04-24T14:30:00`、`2025-04-24 14:30`、`2025/04/24 14:30`、`2025-04-24`、`2025/04/24`、`20250424143000`、`20250424`。

**标红效果**：每组中时间最新的行，所有单元格会被设置为红色背景填充（`#FF0000`），在 Excel 中打开即可直观看到高亮。

**输出文件**：`{原文件名}_标红最新.xlsx`。

---

### 功能 8 · JSONL 解析为 Excel 文件

将 `.jsonl` 文件逐行解析为 Excel 表格，第一层键名自动作为表头。

**使用流程**：工具列出当前目录下的 jsonl 文件 → 选择文件 → 输入输出文件名（默认与 jsonl 同名，扩展名改为 .xlsx） → 确认后执行解析。

**格式化细节**：表头字体为宋体加粗 11 号，数据字体宋体 10 号，所有单元格加细边框，含换行的单元格自动启用换行显示，行高 14，列宽根据前 200 行内容自适应（最大 60 字符）。嵌套 dict 会展开为 `{key:value}` 多行文本，list 值会序列化为 JSON 字符串。空行和非 dict 行自动跳过。

**输出文件**：用户自定义，默认 `{jsonl文件名}.xlsx`。

---

## 输出文件命名速查表

| 功能 | 命名规则 | 示例 |
|------|----------|------|
| 数据提取 | `{原文件名}_处理后.xlsx` | `data_处理后.xlsx` |
| 去重 | `{原文件名}_去重_{列字母}列.xlsx` | `data_去重_A列.xlsx` |
| 筛选交集 | `{A文件名}_筛选结果.xlsx` | `data_筛选结果.xlsx` |
| 批量合并 | 用户自定义，默认 `合并结果.xlsx` | `合并结果.xlsx` |
| 按行分割 | `{原文件名}_第{N}份.xlsx` | `data_第1份.xlsx` |
| 横向合并 | `{A文件名}_横向合并.xlsx` | `data_横向合并.xlsx` |
| 分组标红 | `{原文件名}_标红最新.xlsx` | `data_标红最新.xlsx` |
| JSONL 转 Excel | 用户自定义，默认 `{jsonl文件名}.xlsx` | `log.xlsx` |

---

## 通用交互特性

**直接回车返回**：所有文件选择提示的默认值为 `q`，直接按回车即可返回上级菜单，无需额外输入。

**无文件时友好提示**：当目录下没有匹配的 xlsx 或 jsonl 文件时，工具会暂停显示提示信息，用户按回车确认后返回主菜单，不会报错或闪退。

**列字母自动大写**：输入列号时支持小写字母，工具会自动转为大写并提示。

**长文件名智能截断**：文件列表中过长的文件名会自动保留首尾、中间用 `...` 省略，并自适应终端宽度。

**进度条与动画**：文件读取、数据处理、文件写入等耗时操作均配有 Rich 进度条（旋转动画 + 进度百分比），直观展示处理进度。

---

## 数据安全特性

**文本格式保护**：所有写入的单元格格式均强制设为文本（`@`），所有值转为字符串后写入，从根本上防止 Excel 将身份证号、订单号等长数字转为科学计数法。

**非法字符清洗**：写入前自动清除 openpyxl 不允许的控制字符（`\x00-\x08`、`\x0B`、`\x0C`、`\x0E-\x1F`），避免写入失败。

**零 pandas 依赖**：v5.0 起完全移除 pandas，所有读写和数据处理均由 openpyxl + 纯 Python 实现，消除了 pandas 版本兼容性问题，安装包体积大幅缩小。

---

## 许可证

MIT License
