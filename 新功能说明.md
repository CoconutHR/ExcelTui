# ExcelTui 新功能说明

## 新增功能

### 1. 去除xlsx指定列的重复项
- **功能说明**：根据用户指定的列进行去重，保留首次出现的行
- **使用流程**：
  1. 选择要去重的Excel文件
  2. 选择用于去重的列（支持A、B、P、AA等列字母格式）
  3. 确认后进行去重处理
  4. 输出文件名格式：`原文件名_去重_列字母列.xlsx`

### 2. 筛选交集（按另一文件顺序筛选）
- **功能说明**：从数据文件中筛选出匹配筛选文件值的行，输出顺序与筛选文件保持一致
- **使用流程**：
  1. 选择数据文件（被筛选的文件）
  2. 选择筛选文件（提供匹配值的文件）
  3. 选择数据文件中用于匹配的列
  4. 选择筛选文件中提供匹配值的列
  5. 确认后进行筛选处理
  6. 输出文件名格式：`原文件名_筛选结果.xlsx`

## 使用方法

在项目根目录运行：
```bash
python -m exceltui
```

或在项目目录运行：
```bash
python -c "from exceltui.main import main; main()"
```

## 主菜单选项

1. **Excel 通用数据提取** - 原有的JSON/键值对/纯文本处理功能
2. **去除xlsx指定列的重复项** - 新功能
3. **筛选交集** - 新功能
4. **退出程序**

## 依赖

确保已安装所需依赖：
```bash
pip install pandas openpyxl rich
```
